# -*- mode: javascript; require-final-newline: nil -*-
# name: for(var i = 0; i < arr.length; i++)
# key: for
# --
for(var ${1:counter} = ${2:0}; $1 < ${3:arr}${4:.length}; $1++) {
  `yas-selected-text`$0
}
