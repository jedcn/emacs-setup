;; This comes from
;;
;; https://raw.github.com/magnars/.emacs.d/master/mac.el
(progn
  (set-face-attribute 'default nil :font "Monaco-16")

  (setq mac-option-modifier 'none)
  (setq mac-command-modifier 'meta)
  (setq ns-function-modifer 'hyper))
